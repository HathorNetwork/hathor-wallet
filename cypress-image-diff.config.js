/**
 * Configurations for the visual regression plugin
 * @see https://cypress.visual-image-diff.dev/getting-started/custom-config-file
 */
const config = {
  ROOT_DIR: 'cypress',
  REPORT_DIR: 'visual-test/reports',
  SCREENSHOTS_DIR: 'visual-test/screenshots'
};

module.exports = config;
